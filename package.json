{
  "name": "rarifuse",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ fusetest",
    "deploy-legacy": "graph deploy --product hosted-service zacel/fusedemo",
    "create-local": "graph create --node http://localhost:8020/ rarifuse",
    "remove-local": "graph remove --node http://localhost:8020/ rarifuse",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rarifuse"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0",
    "as-http": "^1.2.0",
    "json-as": "^0.1.7",
    "web3": "^1.5.1"
  }
}
